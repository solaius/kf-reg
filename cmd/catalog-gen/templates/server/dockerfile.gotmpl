# Generated by catalog-gen build-server. Customize as needed.
FROM golang:{{.GoVersion}}-alpine AS builder

RUN apk add --no-cache git

WORKDIR /build

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 go build -o /catalog-server .

FROM gcr.io/distroless/static:nonroot
COPY --from=builder /catalog-server /catalog-server
USER nonroot:nonroot
ENTRYPOINT ["/catalog-server"]
